import { Center, Spinner } from "@volocopter/design-library-react";

export const LoadingScreen = () => {
    return (
        <Center height="full">
            <Spinner />
        </Center>
    );
};
