import { createCompoundComponent } from "@voloiq/utils";

export const {
    CompoundComponent: TimeSchedulerRowContent,
    getCompoundComponentChildren: getTimeSchedulerRowContentChildren,
} = createCompoundComponent();
